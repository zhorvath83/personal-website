---
version: "3"

vars:
  ROOT_DIR:
    sh: "git rev-parse --show-toplevel"

includes:
  hugo: .taskfiles/Hugo/Tasks.yaml
  resume: .taskfiles/Resume/Tasks.yaml
  precommit: .taskfiles/PreCommit/Tasks.yaml

tasks:
  default:
    silent: true
    cmds:
      - task --list
